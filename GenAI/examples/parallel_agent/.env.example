# Parallel Agent — Environment Variables
# Copy this file to .env in the GenAI/ directory (one already exists there)

# OpenAI API key — required
OPENAI_API_KEY=sk-...

# Override the model (optional — defaults to gpt-4o-mini)
# OPENAI_MODEL=gpt-4o

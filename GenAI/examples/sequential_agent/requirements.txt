# Sequential Agent Pattern — Dependencies
# ⚠️  Pin agent-framework while it is in preview to avoid breaking changes

# Microsoft Agent Framework (preview — pin the version!)
agent-framework-azure-ai==1.0.0b260107
agent-framework-core==1.0.0b260107

# Azure Identity (async credential for Foundry / Azure AI)
azure-identity>=1.17.0

# Environment management
python-dotenv>=1.0.0

# Beautiful terminal output
rich>=13.7.0

# Async HTTP (used by azure-identity internally)
aiohttp>=3.9.0
